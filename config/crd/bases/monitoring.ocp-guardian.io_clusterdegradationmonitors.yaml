---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: clusterdegradationmonitors.monitoring.ocp-guardian.io
spec:
  group: monitoring.ocp-guardian.io
  names:
    kind: ClusterDegradationMonitor
    listKind: ClusterDegradationMonitorList
    plural: clusterdegradationmonitors
    shortNames:
    - cdm
    singular: clusterdegradationmonitor
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.overallHealth
      name: Health
      type: string
    - jsonPath: .status.degradedOperatorCount
      name: Degraded
      type: integer
    - jsonPath: .status.unavailableOperatorCount
      name: Unavailable
      type: integer
    - jsonPath: .status.lastCorrelationTime
      name: Last Check
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterDegradationMonitor is the Schema for the clusterdegradationmonitors
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterDegradationMonitorSpec defines the desired state of
              ClusterDegradationMonitor.
            properties:
              checkIntervalMinutes:
                default: 2
                description: CheckIntervalMinutes controls how often the controller
                  checks ClusterOperator status.
                format: int32
                minimum: 1
                type: integer
              degradedThreshold:
                default: 3
                description: DegradedThreshold is the number of degraded ClusterOperators
                  that triggers a Critical overall health.
                format: int32
                minimum: 1
                type: integer
              operatorFilter:
                description: OperatorFilter limits which ClusterOperators to monitor.
                  Empty means all.
                items:
                  type: string
                type: array
              suspend:
                default: false
                description: Suspend stops cluster degradation monitoring when true.
                type: boolean
            type: object
          status:
            description: ClusterDegradationMonitorStatus defines the observed state
              of ClusterDegradationMonitor.
            properties:
              clusterOperators:
                description: ClusterOperators contains the health status of all monitored
                  ClusterOperators.
                items:
                  description: ClusterOperatorHealth describes the health status of
                    a single ClusterOperator.
                  properties:
                    available:
                      description: Available indicates whether the operator is available.
                      type: boolean
                    degraded:
                      description: Degraded indicates whether the operator is degraded.
                      type: boolean
                    lastTransitionTime:
                      description: LastTransitionTime is the last time any condition
                        on this operator transitioned.
                      format: date-time
                      type: string
                    message:
                      description: Message provides a human-readable status message.
                      type: string
                    name:
                      description: Name of the ClusterOperator.
                      type: string
                    progressing:
                      description: Progressing indicates whether the operator is progressing
                        toward a new state.
                      type: boolean
                    version:
                      description: Version is the currently deployed version of this
                        operator.
                      type: string
                  required:
                  - available
                  - degraded
                  - name
                  - progressing
                  type: object
                type: array
              conditions:
                description: Conditions represent the latest available observations
                  of the monitor's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              degradedOperatorCount:
                description: DegradedOperatorCount is the number of degraded ClusterOperators.
                format: int32
                type: integer
              lastCorrelationTime:
                description: LastCorrelationTime is the timestamp of the last health
                  correlation.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the spec
                  last processed.
                format: int64
                type: integer
              overallHealth:
                description: 'OverallHealth summarizes cluster health: Healthy, Warning,
                  or Critical.'
                type: string
              progressingOperatorCount:
                description: ProgressingOperatorCount is the number of progressing
                  ClusterOperators.
                format: int32
                type: integer
              unavailableOperatorCount:
                description: UnavailableOperatorCount is the number of unavailable
                  ClusterOperators.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
