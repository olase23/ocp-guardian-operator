---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: certificateexpirymonitors.monitoring.ocp-guardian.io
spec:
  group: monitoring.ocp-guardian.io
  names:
    kind: CertificateExpiryMonitor
    listKind: CertificateExpiryMonitorList
    plural: certificateexpirymonitors
    shortNames:
    - cem
    singular: certificateexpirymonitor
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.expiringCount
      name: Expiring
      type: integer
    - jsonPath: .status.expiredCount
      name: Expired
      type: integer
    - jsonPath: .status.totalCertificatesScanned
      name: Total Scanned
      type: integer
    - jsonPath: .status.lastScanTime
      name: Last Scan
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CertificateExpiryMonitor is the Schema for the certificateexpirymonitors
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CertificateExpiryMonitorSpec defines the desired state of
              CertificateExpiryMonitor.
            properties:
              checkIntervalMinutes:
                default: 60
                description: CheckIntervalMinutes controls how often the controller
                  scans for expiring certificates.
                format: int32
                minimum: 1
                type: integer
              criticalThresholdDays:
                default: 7
                description: CriticalThresholdDays is the number of days before expiry
                  to raise a critical alert.
                format: int32
                minimum: 1
                type: integer
              excludeNamespaces:
                description: ExcludeNamespaces is a list of namespaces to skip during
                  scanning.
                items:
                  type: string
                type: array
              namespaceSelector:
                description: NamespaceSelector limits which namespaces to scan. Empty
                  means all namespaces.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              suspend:
                default: false
                description: Suspend stops certificate scanning when true.
                type: boolean
              warningThresholdDays:
                default: 30
                description: WarningThresholdDays is the number of days before expiry
                  to raise a warning.
                format: int32
                minimum: 1
                type: integer
            type: object
          status:
            description: CertificateExpiryMonitorStatus defines the observed state
              of CertificateExpiryMonitor.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the monitor's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              expiredCount:
                description: ExpiredCount is the number of certificates that have
                  already expired.
                format: int32
                type: integer
              expiringCertificates:
                description: ExpiringCertificates contains all certificates that are
                  expiring or expired.
                items:
                  description: CertificateInfo describes a single certificate that
                    is expiring or expired.
                  properties:
                    daysUntilExpiry:
                      description: DaysUntilExpiry is the number of days until the
                        certificate expires (negative if already expired).
                      format: int32
                      type: integer
                    expiryDate:
                      description: ExpiryDate is the certificate's NotAfter timestamp.
                      format: date-time
                      type: string
                    issuer:
                      description: Issuer is the certificate's issuer common name.
                      type: string
                    namespace:
                      description: Namespace where the Secret resides.
                      type: string
                    secretName:
                      description: SecretName is the name of the TLS Secret containing
                        this certificate.
                      type: string
                    severity:
                      description: 'Severity is the alert level: Warning, Critical,
                        or Expired.'
                      type: string
                    subject:
                      description: Subject is the certificate's subject common name.
                      type: string
                  required:
                  - daysUntilExpiry
                  - expiryDate
                  - namespace
                  - secretName
                  - severity
                  type: object
                type: array
              expiringCount:
                description: ExpiringCount is the number of certificates within the
                  warning or critical threshold.
                format: int32
                type: integer
              lastScanTime:
                description: LastScanTime is the timestamp of the last certificate
                  scan.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the spec
                  last processed.
                format: int64
                type: integer
              totalCertificatesScanned:
                description: TotalCertificatesScanned is the number of TLS secrets
                  examined in the last scan.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
